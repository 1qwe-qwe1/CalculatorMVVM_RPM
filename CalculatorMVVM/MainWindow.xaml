<Window x:Class="CalculatorMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculatorMVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Калькулятор"
                   FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>
        
        <TextBox Grid.Row="1" 
                 Text="{Binding Expression, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="16" Padding="6" Margin="0,5,0,5"
                 ToolTip="Введите выражение (цифры, + - * / и скобки)"/>

        <Button Grid.Row="2" 
                Content="Вычислить"
                Command="{Binding CalculateCommand}"
                FontSize="14" Padding="8,4" Margin="0,5,0,5"
                HorizontalAlignment="Left" Width="100"/>

        <Border Grid.Row="3" 
                BorderBrush="Gray" BorderThickness="1" 
                CornerRadius="3" Background="#F5F5F5" Margin="0,5,0,5">
            <Grid>
                <TextBlock Text="Результат:" Margin="5,3,0,3" FontWeight="Bold"/>
                <TextBox Text="{Binding Result, Mode=OneWay}"
                         IsReadOnly="True" Background="Transparent"
                         BorderThickness="0" FontSize="18" FontWeight="Bold"
                         Margin="80,3,5,3"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="4" 
                   Text="{Binding ErrorMessage, Mode=OneWay}"
                   Foreground="Red" Margin="0,5,0,5"
                   TextWrapping="Wrap"/>

        <Grid Grid.Row="5" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <TextBlock Text="История" FontWeight="Bold"/>
                <Button Content="Очистить" 
                        Command="{Binding ClearHistoryCommand}"
                        HorizontalAlignment="Right" Width="70"
                        Padding="4,2"/>
            </Grid>
            <ListBox Grid.Row="1" ItemsSource="{Binding History}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Timestamp}" 
                                           FontSize="11" Foreground="Gray"/>
                                <TextBlock Grid.Column="1" Text="{Binding Expression}" 
                                           Margin="8,0" FontSize="13"/>
                                <TextBlock Grid.Column="2" Text="{Binding Result}" 
                                           FontWeight="Bold" Foreground="#1E5A2E"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
